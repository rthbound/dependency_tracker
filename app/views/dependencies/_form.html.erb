<%= form_for(@dependency) do |f| %>
  <% if @dependency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dependency.errors.count, "error") %> prohibited this dependency from being saved:</h2>

      <ul>
      <% @dependency.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :license %><br />
    <%= f.text_area :license %>
  </div>
  <div class="field">
    <%= f.label :license_description %><br />
    <%= f.text_field :license_description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
